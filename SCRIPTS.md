# Скрипты проекта

Документация по вспомогательным скриптам для администрирования и управления проектом.

---

## Создание администратора

**Файл:** `scripts/create-admin.ts`

**Описание:**  
Создает пользователя с ролью `admin` для доступа к админ-панели Payload CMS (`/admin`).  
Если пользователь с указанным email уже существует, обновляет его роль на `admin`.

**Учетные данные (хардкод в скрипте):**
- Email: `col1596321@gmail.com`
- Пароль: `11559966332211kkKK`

**Запуск:**
```bash
pnpm tsx scripts/create-admin.ts
```

**Требования:**
- Должен быть настроен `.env` файл с переменными `DATABASE_URL` и `PAYLOAD_SECRET`
- База данных должна быть доступна

**Пример вывода:**
```
🚀 Подключение к Payload CMS...
✅ Подключение установлено
📧 Email: col1596321@gmail.com
✅ Администратор создан успешно (ID: 123)

📋 Данные для входа:
   URL: http://localhost:3000/admin
   Email: col1596321@gmail.com
   Пароль: 11559966332211kkKK

🎉 Готово!
```

---
